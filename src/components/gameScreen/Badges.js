import React from 'react';
import Badge from './Badge';

const Badges = () => {
    return(
        <div>
            <Badge value={0} />
            <Badge value={1} />
        </div>
    )
}

export default Badges;